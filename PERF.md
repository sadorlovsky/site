# Производительность

Состояние работ по скорости сайта. Отправная точка — исследование августа 2026:
жалобы на «тормозящий вишлист» и «медленные переходы между страницами».

Сделанное лежит на ветке `first-paint` (6 коммитов) и **не задеплоено**.

Замеры делались из Алматы. Важная поправка на неё: пол по TTFB — 287 мс
(`/favicon.svg`, 519 байт чистой статики с эджа во Франкфурте), тёплый TTFB
страниц — 308–347 мс. То есть на попадании в кэш сервер добавляет 21–60 мс, а
всё остальное — три сетевых круга до `fra1`. Серверный рендер и Turso в
проблеме не участвуют: вишлист делает 4 запроса в `Promise.all`, суммарно
6.12 мс локально, N+1 нет нигде, функция в `dub1` и база в `aws-eu-west-1` —
оба Дублин.

## Что уже сделано

| Коммит | Что |
|---|---|
| `208a6f4` | `immutable` для `/_astro`; языковое скрытие сужено с контейнера до переводимого текста; фотографии перестали ждать JS; кнопки Reserve не ждут сеть |
| `de29c7a` | Матрица вариантов картинок 748 → 372 |
| `ed8c2d2` | `/travel`: maplibre и его CSS по требованию, флаги файлами вместо инлайна |
| `7648548` | Скролл вишлиста: `will-change` по требованию, кэш геометрии, композитный шиммер |
| `6241f57` | Починка двух регрессий: своя бронь после перезагрузки, мигание цен долларами |

---

# Осталось сделать

## 5. Картинки мимо serverless-функции

**Текущее состояние:** у адаптера нет `imageService: true`, поэтому
`/_vercel/image` отвечает 404, а работает встроенный sharp внутри `_render`.
Каждый промах кэша — скачать 137 КБ из R2, пережать, отдать. Замеры холодных
промахов на проде: 9.54 / 7.88 / 5.29 / 5.22 / 4.44 с. Тёплое попадание —
0.28–0.40 с, прямая отдача из R2 — 0.48–0.87 с. Сам R2 за `cdn.orlovsky.dev`
работает идеально (`cf-cache-status: HIT`, age 4.4 дня) — браузер просто
никогда туда не ходит.

Коммит `de29c7a` сократил **число** промахов вдвое, но не их **цену**.

**Решение:** три варианта по возрастанию трудозатрат.

1. `vercel({ imageService: true })` — одна строка. Переводит на
   `/_vercel/image` с выделенным кэшем, перестаёт будить SSR-функцию.
   Быстрее, чем сейчас, но всё ещё не так быстро, как отдача прямо с
   Cloudflare.
2. Прегенерация деривативов при загрузке в админке, рядом с оригиналом в R2.
   Рантайм-стоимость нулевая.
3. Cloudflare Image Resizing / Cloudflare Images на `cdn.orlovsky.dev`, `<img
   src>` смотрит прямо туда.

**Приоритет:** высокий. Единственное, что снимает саму цену промаха.

---

## 6. `prefetch` + `ClientRouter`

**Текущее состояние:** `ClientRouter` не импортируется нигде (grep по `src/`
пуст), секции `prefetch` в `astro.config.mjs` нет, в `Dock.astro:42-52`
обычные `<a href>` без `data-astro-prefetch`. Каждый клик — полная загрузка
документа, снос DOM, перепарс CSS, перезапуск инлайновых скриптов.

Усугубляется тем, что CSS не переиспользуется между маршрутами: `PageHeader`
собран в два разных хэша — свой для `/travel` и свой для `/wishlist`.

**Решение:** `prefetch: { prefetchAll: true, defaultStrategy: "hover" }` в
конфиге и `<ClientRouter />` в `PageLayout.astro`. Инлайновые скрипты в
`src/client/` придётся перевести с `DOMContentLoaded` на `astro:page-load`.

**Зависимость:** имело смысл только после `immutable` (коммит `208a6f4`) — при
`max-age=0` предзагруженный документ всё равно требовал круга по сети.

**Приоритет:** высокий. Прямой ответ на жалобу о медленных переходах.

---

## 8. `backdrop-filter` на кнопках — остаток пункта

**Текущее состояние:** `.reserve-btn` несёт `backdrop-filter: blur(20px)
saturate(150%)` (`wishlist.css:766`, токен `--glass-filter` в
`global.css:28`), и таких кнопок на странице 57. Это самый дорогой эффект из
возможных: браузер обязан прочитать пиксели под элементом и размыть их,
заново при каждом сдвиге — то есть на протяжении всего скролла.

Остальные части пункта 8 сделаны в `7648548`.

**Решение:** сначала **измерить, виден ли эффект вообще**. Кнопка сидит в
футере карточки, а не поверх фотографии — под ней сплошной фон карточки, и
размытие сплошной заливки даёт ту же заливку. Проверяется двумя скриншотами
одной карточки, с фильтром и без, и попиксельным сравнением.

- разницы нет → непрозрачный фон, эффект не теряется;
- разница видна → оставить, но под `@media (hover: hover)` и `:hover`, чтобы
  платить только под курсором.

**Приоритет:** средний, и это **решение владельца**: «жидкое стекло» проходит
через весь сайт, менять его молча нельзя.

**Не трогать:** `content-visibility: auto` (`wishlist.css:248`). Отчёт
предлагал пересмотреть `contain-intrinsic-size`, но парой строк ниже есть
`@supports`-ветка с `auto 300px auto 400px` — браузер запоминает реальный
размер карточки, прыжки скроллбара уже вылечены.

---

# Хвост находок вне основного плана

## `stale-while-revalidate` для HTML

Самое прямое средство от «медленных переходов»: браузер рисует страницу из
своей копии сразу и обновляет её фоном.

Для `/`, `/blog`, `/travel` включается сразу. Для `/wishlist` и категорий
сначала нужен `expiration` в конфиге ISR: по документации адаптера, при
включённом ISR без него Vercel использует cache shielding и **любые
`Cache-Control` игнорирует**.

## `CDN-Cache-Control: no-store` на вишлисте

`wishlist/index.astro:7` и `wishlist/[category].astro:7`. ISR кэширует
независимо от этого заголовка (`x-vercel-cache: HIT` приходит), но эдж во
Франкфурте не имеет права держать копию, поэтому каждый заход идёт
`fra1::dub1::` вместо `fra1::`. Цена измерена: **+39 мс** к p50.

## `preload` для LCP-картинки и жадная загрузка

Первая картинка получает `fetchpriority="high"`, но обнаруживается только
после парсинга 446 КБ HTML. Нужен `<link rel="preload" as="image"
imagesrcset imagesizes>` в `<head>`.

Там же: `eager={index < 6}` (`WishlistPage.astro:149`) — на узком экране видно
1–2 карточки, а шесть жадных загрузок конкурируют с LCP за полосу. Разумно
снизить до 2–3.

## Тройной прогон i18n

Переводы применяются трижды: `LangInit.astro` (4 обхода: 316 + 191 + 59
узлов), инлайн-скрипт `WishlistPage.astro` (63 цены) и `client/wishlist.ts`
(63 + 316 + 59 + 57×2). Итого около 1500 записей `textContent`/атрибутов за
восемь полных обходов документа, примерно половина — точный повтор уже
сделанного. Плюс 126 конструирований `Intl.NumberFormat` в цикле
(`WishlistPage.astro:59`, `wishlist.ts:396`) — форматтер стоит закэшировать в
модульной переменной.

## Заголовки безопасности не доезжают до статики

`X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy` ставятся в
`src/middleware.ts:8-10`, но middleware не выполняется для prerendered-страниц
— в `.vercel/output/functions/` его функции нет вовсе, он вкомпилен в
Node-функцию, а на ISR-попадании она не вызывается. Проверено: у `/` этих
заголовков в ответе нет, у `/wishlist` есть.

Не про скорость, но чинится тем же блоком заголовков, что и `immutable`.

---

# Исправлено, но не проверено

## Ничего не задеплоено

Ветка `first-paint` не запушена. Пока этого не произошло, **не подтверждены**:

- **Пункт 1, иммутабельные заголовки.** Их ставит роутинг Vercel, локально
  воспроизвести нельзя. После деплоя: `curl -I` по
  `/_astro/fonts/*.woff2` должен показать `max-age=31536000, immutable`
  вместо нынешнего `max-age=0, must-revalidate`.
- **Пункт 4, выгода по кэшу картинок.** Сокращение матрицы 748 → 372 имеет
  смысл только на реальном CDN: на проде 12 случайных вариантов давали 12
  промахов из 12.
- **TTFB и ощущение переходов** в целом.

## Локальная проверка пройдена не до конца

Владелец прошёл шаги 1 и 2 чеклиста (язык и кнопки) — обе найденные там
регрессии починены в `6241f57`. Не пройдены:

- **шаг 3** — скролл и скелетоны: paint flashing, панель Layers, плавность;
- **шаг 4** — `/travel`: глобус, стили контролов, флаги отдельными файлами,
  зум и наведение на города;
- **шаг 5** — картинки: только webp, ширины 400/560/800, тёмная тема;
- **шаг 6** — прогон на собранной копии.

## Магнитное притяжение кнопки

Переписано в `7648548` (`will-change` по требованию, кэш геометрии), но
**поведение не проверено ни разу**: вся анимация построена на
`requestAnimationFrame`, а он заморожен в фоновых вкладках — в среде, где шла
разработка, кнопка не двигается и слой не снимается. Логика прямая
(`pointerleave` запускает возврат, тот доводит до нуля и снимает
`will-change`), но подвигать мышью над кнопкой должен человек.

По той же причине не проверялась карта: `IntersectionObserver` не вызывает
колбэки на скрытой странице, поэтому `/travel` в фоновой вкладке не строит
глобус вовсе. Работоспособность подтверждена принудительным вызовом на
собранной копии — canvas создаётся, `setMapMode` появляется,
`.maplibregl-canvas` получает `position: absolute` от подключённой на лету
таблицы стилей, — но вращающийся глобус глазами никто не видел.

## Тёмные версии картинок проверены синтетически

В локальном сиде нет ни одного товара с `imageUrlDark`, тогда как на проде их
61 из 63. Чтобы проверить ветку с `<picture>`, поле временно проставлялось
одному товару прямо в `local.db`, после чего база возвращалась в исходное
состояние. Разметка верна (один media-gated `<source type="image/webp">`), но
на реальных тёмных файлах это не гонялось.

## Прочее, оставшееся без внимания

- В корне лежит неотслеживаемый файл `:memory:` — похоже на артефакт от
  строки подключения к БД, попавшей в путь.
- `db/migrations/meta/0001_snapshot.json` не закоммичен, хотя, судя по
  устройству миграций, должен идти вместе с миграцией `0001`.
- В `TravelMap.astro` импортируется `flag-icons`, хотя карта флаги не
  использует; стили `.city-label-flag` и `.flag-fix` там же — мёртвые.
