---
interface Props {
  date?: string;
}

const { date } = Astro.props;
---

<div class="change-marker updated">
  {date && <span class="change-date">{date}</span>}
  <slot />
</div>

<style>
  .change-marker.updated {
    position: relative;
    margin: 1.5rem 0;
    padding: 1rem 1.25rem;
    border-left: 3px solid #eab308;
    background: linear-gradient(to right, rgba(234, 179, 8, 0.08), transparent 50%);
    border-radius: 0 8px 8px 0;
  }

  .change-date {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 500;
    color: #eab308;
    background: rgba(234, 179, 8, 0.15);
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  .change-marker.updated :global(h3:first-of-type) {
    margin-top: 0;
  }

  .change-marker.updated :global(p:last-child),
  .change-marker.updated :global(pre:last-child) {
    margin-bottom: 0;
  }
</style>
