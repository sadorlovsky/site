---
interface Props {
  date?: string;
}

const { date } = Astro.props;
---

<div class="change-marker removed">
  {date && <span class="change-date">{date}</span>}
  <slot />
</div>

<style>
  .change-marker.removed {
    position: relative;
    margin: 1.5rem 0;
    padding: 1rem 1.25rem;
    border-left: 3px solid #ef4444;
    background: linear-gradient(to right, rgba(239, 68, 68, 0.08), transparent 50%);
    border-radius: 0 8px 8px 0;
    opacity: 0.7;
  }

  .change-date {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 500;
    color: #ef4444;
    background: rgba(239, 68, 68, 0.15);
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  .change-marker.removed :global(h3:first-of-type) {
    margin-top: 0;
    text-decoration: line-through;
  }

  .change-marker.removed :global(p) {
    text-decoration: line-through;
  }

  .change-marker.removed :global(p:last-child),
  .change-marker.removed :global(pre:last-child) {
    margin-bottom: 0;
  }

  .change-marker.removed :global(code) {
    text-decoration: line-through;
  }
</style>
